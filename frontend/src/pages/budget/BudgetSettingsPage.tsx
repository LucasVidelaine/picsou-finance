import { ManageTab } from './ManageTab'

/**
 * `/budget/settings` — categories & subcategories, pay cycle, and (M5) the opt-in
 * brand-logo toggle. Hosts the existing manage surface for now.
 */
export function BudgetSettingsPage() {
  return <ManageTab />
}
