# TODO

Scratch list of deferred work noticed in passing. Not durable knowledge — delete once resolved.

- 2026-07-15: `AccountsVisibilityTab.tsx` has no component test, unlike sibling `TradeRepublicTab.test.tsx`. It's mostly presentational but hinges on two boolean inversions (`checked={!account.hidden}`, `onCheckedChange={(visible) => toggle.mutate({ hidden: !visible })}`) — classic double-negation footgun, cheap to pin down with a render+toggle test. Flagged non-blocking by the final review on the account-visibility feature (docs/briefs/2026-07-14-account-visibility-toggle-plan.md).
- 2026-07-15: `ASSET_FILTER_MAP` filtering (`const types = ASSET_FILTER_MAP[filter]; if (!types) return X; return X.filter(...)`) is duplicated 3× in `frontend/src/pages/accounts/AccountsPage.tsx` after the `useAccountTree` refactor. A small `filterByAssetType(list)` helper would DRY it without over-abstracting. Flagged Minor/non-blocking by the same final review.
- 2026-07-18: `backend/src/main/java/com/picsou/mcp/Scopes.java`'s class Javadoc still claims "Read scopes end in `:read`; everything else mutates" — false since `budget:*-read` uses a hyphen and `oauth2:discover`/`oauth2:session-status` carry no read suffix at all. This is the exact wrong assumption PR #56 just fixed on the frontend (`scopeGroup()` in `frontend/src/features/accessKeys/scopes.ts`); the stale backend comment could reseed the same bug if someone mirrors it. Update the Javadoc to describe all 3 naming patterns.
- 2026-07-18: Frontend `scopeGroup()` re-derives read/write from scope-string naming patterns (3 different conventions now) instead of the backend telling it directly. Works fine at 22 scopes with `READ_ONLY_OVERRIDES` as an escape hatch, but a deeper fix would have the backend `Scopes.ALL` expose read/write as data (e.g. a `Map<String, ScopeGroup>` or a `Set` per group) so the frontend catalog can't drift from backend semantics again. Worth it if the scope vocabulary keeps growing with irregular naming; not worth it today.
- 2026-07-22: `CHANGELOG.md`'s `## [1.1.0] — 2026-06-09` header predates `1.0.9`–`1.0.13` (2026-06-27 to 2026-07-07), discovered while merging `main` into `1.1.0`. The date was presumably stamped when the budget branch started, never updated. Needs a real release date (or `[Unreleased]`) once 1.1.0 is actually tagged.
- 2026-07-22: `BudgetSeedWriteOnReadPostgresTest.java`'s class Javadoc claims it's "the project's only container-backed test" — false as of the `main` merge, which added `OAuth2SchemaMigrationTest`, `WalletEvmMigrationTest`, and the whole OAuth2/MCP `@Testcontainers` suite (11 classes total). Update the comment to stop claiming uniqueness.
